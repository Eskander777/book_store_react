const initialState = {
    completeOrder: {
        order: [],
        orderTotal: {
            totalPrice: 0,
            totalAmount: 0
        }
    }
}

const reducer = (state = initialState, action) => {
    return state;
}

export default reducer;